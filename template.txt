<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Situs menipu di depan</title>
    <style>
        /* --- CSS Global & Desktop --- */
        :root {
            --google-red: #c7362f;
            --google-blue: #1a73e8;
            --text-primary: #202124;
            --text-secondary: #5f6368;
            --bg-color: #f8f9fa;
            --border-color: #dadce0;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            height: 100%;
            background-color: var(--google-red);
            font-family: 'Roboto', 'Arial', sans-serif;
            -webkit-font-smoothing: antialiased;
        }

        body {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .container {
            background-color: #fff;
            padding: 48px;
            border-radius: 8px;
            max-width: 680px;
            width: 100%;
        }

        .header {
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .header .icon {
            width: 48px;
            height: 48px;
        }

        .header h1 {
            font-size: 24px;
            font-weight: 500;
            color: var(--google-red);
        }
        
        .content {
            margin-top: 24px;
            color: var(--text-primary);
            font-size: 14px;
            line-height: 1.5;
        }

        .content p {
            margin-bottom: 16px;
        }
        
        .content strong {
            font-weight: 500;
        }

        .actions {
            margin-top: 32px;
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .button {
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
            border-radius: 4px;
            padding: 8px 24px;
            transition: background-color 0.2s;
        }

        .button-primary {
            background-color: var(--google-blue);
            color: #fff;
            border: 1px solid transparent;
        }
        .button-primary:hover {
            background-color: #185abc;
        }

        .button-secondary {
            color: var(--google-blue);
            background-color: transparent;
            border: 1px solid var(--border-color);
        }
        .button-secondary:hover {
            background-color: #f8f9fa;
        }
        
        /* --- CSS Responsif untuk Mobile --- */
        @media (max-width: 600px) {
            body {
                padding: 0;
                align-items: flex-start; /* Konten mulai dari atas di mobile */
            }

            .container {
                max-width: 100%;
                height: 100%;
                border-radius: 0;
                padding: 24px;
                display: flex;
                flex-direction: column;
                justify-content: center; /* Pusatkan konten secara vertikal di mobile */
            }

            .header h1 {
                font-size: 20px;
            }

            .actions {
                flex-direction: column;
                align-items: flex-start;
                gap: 12px;
            }
            
            .button {
                width: 100%;
                text-align: center;
                padding: 12px 24px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header class="header">
            <svg class="icon" viewBox="0 0 24 24" fill="#c7362f">
                <path d="M1 21h22L12 2 1 21zm12-3h-2v-2h2v2zm0-4h-2v-4h2v4z"/>
            </svg>
            <h1>Situs menipu di depan</h1>
        </header>

        <div class="content">
            <p>
                Penyerang di situs <strong><?php echo htmlspecialchars($_SERVER['HTTP_HOST']); ?></strong> dapat menipu Anda untuk melakukan sesuatu yang berbahaya seperti menginstal software atau mengungkapkan informasi pribadi Anda (misalnya, sandi, nomor telepon, atau kartu kredit).
            </p>
            <p>
                Google Safe Browse baru-baru ini mendeteksi phishing di <strong><?php echo htmlspecialchars($_SERVER['HTTP_HOST']); ?></strong>. Situs phishing meniru situs lain yang Anda percayai untuk menipu Anda agar mau mengungkapkan informasi pribadi atau keuangan.
            </p>
        </div>

        <div class="actions">
            <a href="javascript:history.back()" class="button button-primary">Kembali ke halaman aman</a>
            <a href="#" class="button button-secondary">Lanjutkan ke situs</a>
        </div>
    </div>
</body>
</html>
